# raylua-shared
A small personal framework, which is my foundation for writing programs using raylib-lua 

Use this commands to make it work properly: 
```sh
git clone https://github.com/klen-list/raylua-shared core
```
or
```sh
git submodule add https://github.com/klen-list/raylua-shared core
```
